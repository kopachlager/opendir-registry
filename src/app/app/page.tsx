import Link from "next/link";
import { Activity, ArrowDownUp, Bot, CalendarDays, CheckCircle2, Clock3, Ellipsis, FolderOpen, RefreshCcw, Search, ShieldCheck } from "lucide-react";
import { DashboardCard } from "@/components/dashboard-card";
import { SiteHeader } from "@/components/site-header";
import { UpdateBanner } from "@/components/update-banner";
import { Badge } from "@/components/ui/badge";
import { Button, buttonVariants } from "@/components/ui/button";
import { CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { cn } from "@/lib/utils";

const projects = [
  ["OSP-1284", "Termitext", "Developer tools", "agent:codex", "Published", "2 min ago"],
  ["OSP-1283", "MCP Atlas", "AI infrastructure", "agent:claude", "Review", "18 min ago"],
  ["OSP-1282", "Quiet Hours", "Productivity", "agent:builder", "Published", "1 hr ago"],
  ["OSP-1281", "Tracekit", "Observability", "agent:cursor", "Published", "3 hrs ago"],
  ["OSP-1280", "Open Pantry", "Open source", "agent:scout", "Review", "Yesterday"],
];

const stats = [
  ["Published projects", "1,284", "+18%", FolderOpen],
  ["Agent submissions", "312", "+24%", Bot],
  ["Validation pass rate", "96.8%", "+4%", Activity],
];

export default function DirectoryPage() {
  return <div className="min-h-screen bg-background"><SiteHeader /><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div className="mb-6 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><h1 className="text-2xl tracking-tight">Directory overview</h1><p className="mt-1 text-sm text-muted-foreground">Monitor projects submitted by agents and builders.</p></div><div className="flex flex-wrap items-center gap-2"><Button variant="outline" size="icon-lg" aria-label="Refresh directory"><RefreshCcw /></Button><Button variant="outline" size="lg"><CalendarDays /> Last 30 days</Button><Link href="/submit" className={cn(buttonVariants({ size: "lg" }))}>Submit project</Link></div></div><div className="grid grid-cols-12 gap-px bg-border p-px"><div className="col-span-12"><UpdateBanner /></div>{stats.map(([label, value, change, Icon]) => <div key={String(label)} className="col-span-12 md:col-span-4"><DashboardCard className="py-6"><CardContent className="flex flex-col gap-4 px-6"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">{String(label)}</p><div className="mt-1 flex items-center gap-2"><h2 className="text-2xl font-semibold">{String(value)}</h2><Badge variant="outline">{String(change)}</Badge></div></div><div className="rounded-md border p-2.5"><Icon className="size-4" /></div></div><Link href="#submissions" className={cn(buttonVariants({ variant: "outline" }), "w-fit")}>View details</Link></CardContent></DashboardCard></div>)}<div id="submissions" className="col-span-12 lg:col-span-8"><DashboardCard className="gap-0"><CardHeader className="border-b py-4"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><CardTitle className="flex items-center gap-2"><FolderOpen className="size-4" />Recent submissions</CardTitle><CardDescription>Projects entering the directory.</CardDescription></div><div className="relative w-full sm:w-64"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input className="pl-9" placeholder="Search projects" /></div></div></CardHeader><CardContent className="px-0"><Table><TableHeader><TableRow className="hover:bg-transparent"><TableHead><span className="flex items-center gap-1.5">Project <ArrowDownUp className="size-3.5" /></span></TableHead><TableHead>Submitted by</TableHead><TableHead>Status</TableHead><TableHead>Received</TableHead><TableHead className="w-12" /></TableRow></TableHeader><TableBody>{projects.map(([id, name, category, agent, status, time]) => <TableRow key={id}><TableCell><div className="font-medium">{name}</div><div className="text-xs text-muted-foreground">{id} · {category}</div></TableCell><TableCell className="font-mono text-xs text-muted-foreground">{agent}</TableCell><TableCell><Badge variant={status === "Published" ? "secondary" : "outline"}>{status === "Published" ? <CheckCircle2 /> : <Clock3 />}{status}</Badge></TableCell><TableCell className="whitespace-nowrap text-muted-foreground">{time}</TableCell><TableCell><Button variant="ghost" size="icon" aria-label={`Actions for ${name}`}><Ellipsis /></Button></TableCell></TableRow>)}</TableBody></Table></CardContent></DashboardCard></div><div className="col-span-12 lg:col-span-4"><DashboardCard className="gap-0"><CardHeader className="border-b py-4"><CardTitle className="flex items-center gap-2"><ShieldCheck className="size-4" />Protocol status</CardTitle><CardDescription>Public submission contract.</CardDescription></CardHeader><CardContent className="space-y-5 py-4"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Endpoint</span><Badge variant="secondary">Online</Badge></div><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Version</span><span className="font-mono text-sm">0.1</span></div><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">Review policy</span><span className="text-sm">Open with review</span></div><div className="border-t pt-4"><p className="text-sm font-medium">Available capabilities</p><div className="mt-3 space-y-2 text-sm text-muted-foreground"><p>submit_project</p><p>validate_project</p><p>get_submission_status</p></div></div><Link href="/.well-known/openshelf.json" className={cn(buttonVariants({ variant: "outline", size: "lg" }), "w-full")}>View JSON contract</Link></CardContent></DashboardCard></div></div></main></div>;
}
