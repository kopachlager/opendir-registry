import { LegalPage } from "@/components/legal-page";

export default function SecurityPage() {
  return <LegalPage eyebrow="Security" title="Security policy" intro="OpenDir separates public listing access from reviewer actions and documents the limits of its current single-owner security model."><section><h2>Reporting a vulnerability</h2><p>Please use a <a href="https://github.com/kopachlager/opendir-registry/security/advisories/new" target="_blank" rel="noreferrer">private GitHub security advisory</a>. Do not include exploit details, credentials, or sensitive submission data in a public issue.</p></section><section><h2>Current controls</h2><ul><li>Reviewer access requires a high-entropy secret stored outside the source repository.</li><li>Reviewer sessions use HTTP-only, Secure, SameSite=Strict cookies with an eight-hour lifetime.</li><li>Review APIs reject unauthenticated requests.</li><li>Public search reads approved project records only.</li><li>Submission identifiers contain unpredictable random values.</li><li>Database review decisions are transactional and recorded in an event history.</li></ul></section><section><h2>Current limitations</h2><p>The initial reviewer model uses one shared administrator secret. It does not yet provide individual OAuth identities, multi-factor authentication, per-reviewer permissions, or login rate limiting. Those are required before granting access to a larger moderation team.</p></section><section><h2>Project verification</h2><p>OpenDir currently validates submission structure and relies on human review of linked deployments. Automated liveness checks are planned, but must be implemented in an isolated verifier with protections against server-side request forgery, unsafe redirects, private network access, and unbounded responses.</p></section></LegalPage>;
}
